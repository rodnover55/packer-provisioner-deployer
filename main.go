package main

import (
	"fmt"
)

func main() {
	//	server, err := plugin.Server()

	//	if err != nil {
	//		panic(err)
	//	}

	//server.RegisterProvisioner(new(deployer.Provisioner))
	//	server.Serve()

	fmt.Println("test")
}
